#include "main.h"

/**
 * _isalpha - checks for alphabeical letters
 * @c: a character to be checked on
 * Return: return 1 0r 0 depending on condition
 */
int _isalpha(int c)
{
	return ((c >=  'a' && c <= 'z') || (C >= 'A' &&  C <= 'Z'));
}
